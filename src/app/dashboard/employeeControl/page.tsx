import { EmployeeControl } from "@/components";

const employeeControl = () => {
  return (
    <div className="flex flex-col items-center justify-center h-screen">
      <EmployeeControl />
    </div>
  );
};
export default employeeControl;
